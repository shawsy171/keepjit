import styled from 'styled-components';
import { SkyBlue } from '../../styles/colors';

export const CardSt = styled.div`
  margin: 0 auto 10px;
  padding: 10px;
  max-width: 50%;
  background-color: ${SkyBlue};
`;